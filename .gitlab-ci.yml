image: electrotumbao/golang-protoc

Linux:
    script:
        - "./scripts/build.sh"
    variables:
        OPERATING_SYSTEM: "linux"
    artifacts:
        paths:
            - dist/

Windows:
    script:
        - "./scripts/build.sh"
    variables:
        OPERATING_SYSTEM: "windows"
    artifacts:
        paths:
            - dist/

OpenBSD:
    script:
        - "./scripts/build.sh"
    variables:
        OPERATING_SYSTEM: "openbsd"
    artifacts:
        paths:
            - dist/

FreeBSD:
    script:
        - "./scripts/build.sh"
    variables:
        OPERATING_SYSTEM: "freebsd"
    artifacts:
        paths:
            - dist/

NetBSD:
    script:
        - "./scripts/build.sh"
    variables:
        OPERATING_SYSTEM: "netbsd"
    artifacts:
        paths:
            - dist/

Darwin:
    script:
        - "./scripts/build.sh"
    variables:
        OPERATING_SYSTEM: "darwin"
    artifacts:
        paths:
            - dist/
